<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User profile</title>
</head>
<body>
  
  <% if (typeof user !== 'undefined') { %>
    <h2><%- user.username %> edit profile page </h2>
  <% } %>

  <% if (typeof message !== 'undefined') { %>
    <h2><%- message %> </h2>
    <p><%- messageClass %> </p>
  <% } %>


</body>
</html>